query GetMovieClips($getMovieClipsId: ID!) {
  getMovieClips(id: $getMovieClipsId) {
    id
    iso_639_1
    iso_3166_1
    name
    key
    site
    size
    type
    official
    published_at
  }
}
