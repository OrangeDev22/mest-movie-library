query SearchMovie($search: String!, $page: Float) {
  searchMovie(search: $search, page: $page) {
    movies {
      id
      adult
      backdrop_path
      title
      original_language
      original_title
      overview
      poster_path
      media_type
      genre_ids
      popularity
      release_date
      video
      vote_average
      vote_count
    }
    total_pages
  }
}
